# AgriSmart backend — Python 3.9+
flask==3.1.0
flask-cors==5.0.0
python-dotenv==1.0.1

# Google Gemini SDK (mới, thay thế google-generativeai đã EOL 30/11/2025)
google-genai==1.5.0
pillow==11.1.0

# Tùy chọn: nhận dạng giọng nói (ghi âm). Bỏ comment nếu cần.
# SpeechRecognition==3.14.1
